{
  "name": "menu",
  "version": "0.0.0",
  "private": true,
  "packageManager": "bun@1.3.4",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "setup:local": "bun install && bun run --cwd packages/backend db:setup:local",
    "dev": "bun run --filter '*' dev",
    "build": "bun run --cwd packages/frontend build && bun run --cwd packages/backend build",
    "test": "bun run --cwd packages/backend test && bun run --cwd packages/frontend test",
    "format": "biome check --write .",
    "typecheck": "bun run --filter '*' typecheck",
    "deploy": "bun run --cwd packages/backend deploy",
    "deploy:preview": "bun run --cwd packages/backend deploy:preview"
  },
  "engines": {
    "bun": "1.3.4"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.11"
  }
}
